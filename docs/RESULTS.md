# RESULTS — Steel Defect Inspection Suite (Severstal)
> 라이브 데모: https://maxwell779.github.io/steel-defect-suite/

> 실제 결과 종합. 모든 평가는 **이미지 단위 fold(누수 차단)**. 상세 EDA는 [EDA.md](EDA.md).
> *기준일: 2026-06-25. M0~M5 완료(밤샘 스윕·교차검증·마스크 정제·5fold·분류게이트).*

## 요약
| 스테이지 | 지표 | 결과 |
|---|---|---|
| 0 누수 폭로 | 패치분류 patch-split vs image-split | acc Δ **+1.25%p**, AUC Δ +0.009 (누수 실측) |
| M1 세그 베이스라인 | UNet/se_resnext50 fold0 mean Dice | 0.9317 → 마스크 정제 0.9343 (**단, C1/C2 0% 붕괴**) |
| M2 클래스 균형 | FocalTversky+오버샘플+posweight | 0.9431, **C1/C2 0%→83%/94% 회복** |
| M3 스윕 승자 | 6arch×5enc + 전처리·손실·HP 그리드 | **DeepLabV3+/effb3 (bce_lovasz) 0.9514** |
| M3 전처리 교차검증 | 상위 전처리 5종 → 승자 적용 | **전부 control(none) 미달** → 전처리 불채택 |
| M4 앙상블+TTA+마스크정제 ★ | 4모델 앙상블 + h/v TTA + 3-임계 | **0.9534** (C1~C4 [0.966,0.992,0.868,0.988]) |
| M5 5-fold | 승자 5-fold(누수無) | **0.9519±0.0006** (견고) |
| M5 분류 게이트 | 멀티라벨 EffNet-B3 | **mAUROC 0.9954**, empty 차단 ~98% @recall95 |
| **M5 게이트 결합(fold0)** | 앙상블+TTA+마스크정제 **+분류게이트** | **0.9559** (Δ +0.0025 vs ungated) |
| ① 5-fold OOF(무누수·일반화) | 전체 train 12568, fold별 승자+분류기 | ungated 0.9524 → **gated 0.9532** (fold 0.9509~0.9532) |
| ② pseudo-label 2R(단일+의사라벨) | test 1397 의사라벨 → train fold0(val격리) | 단일 0.9514 → R1 0.9518 → **R2 0.9540** (+0.0026) |
| ③ 무라벨 이상탐지 | PatchCore-lite(ImageNet 특징) | **AUROC ~0.45(반전·실패)** → 무라벨 부적합(작동 안 함) |
| **최종: 5모델 앙상블+TTA+마스크정제+게이트** | 위 4모델 + **pseudo R2** 멤버 추가 | **0.9573** (ungated 0.9558, C3 0.878) |

> 참고: Severstal 공개 리더보드 상위권은 private test 기준 ~0.90–0.918. 본 수치는 **train 이미지 단위 held-out fold** 기준이라 리더보드와 직접 비교는 아니지만, **단일모델·단일fold 베이스라인으로 매우 강함**.

## Stage 0 — 패치분류 누수 폭로
다른 팀(KDT Vision-Q)은 세그멘테이션을 **256×256 패치 이진분류**로 우회했고, 50% overlap 패치를 분할하면 누수 위험이 있다(그들 스스로 "NB08 90.9% vs NB12 93.3% 분할차" 보고). 이를 동일 분류기로 정량화:

| 분할 | AUC | acc | f1 |
|---|---|---|---|
| patch-split (누수) | 0.9821 | 0.9514 | 0.8800 |
| image-split (정상) | 0.9735 | 0.9389 | 0.8510 |
| **Δ 누수 인플레** | +0.009 | **+1.25%p** | +0.029 |

- 누수는 **실재하지만 modest**(이진 패치 수준). → 핵심 문제는 누수 크기가 아니라 **그들이 과제 본질(픽셀 Dice + empty-FP 억제)을 통째로 비껴갔다**는 점.
- 코드: `src/patch_leak.py` (4000장·4ep, resnet18).

## Stage 2 — 세그멘테이션 베이스라인 ★
- 모델: **smp UNet + se_resnext50_32x4d**(imagenet), in=3 / out=4(멀티라벨 마스크), 입력 256×1600 풀해상도.
- 학습: BCE+Dice(0.6/0.4), AdamW lr 3e-4, cosine 12ep, AMP, bs16, A100.
- 평가: **대회식 (이미지×클래스) mean Dice**, 이미지 단위 fold0(train 10054 / val 2514).

| | mean Dice | C1 | C2 | C3 | C4 |
|---|---|---|---|---|---|
| best (no postproc) | **0.9317** | 0.928 | 0.981 | **0.835** | 0.983 |
| + min-size 정제(800) | **0.9343** | | | | |

- 코드: `src/train_seg.py`, `src/metrics.py`(대회 Dice), `src/data.py`(RLE↔mask).

### ⚠️ M1.5 분해 분석 — mean Dice 0.934의 함정
대회 Dice를 **빈마스크 억제 / 결함 검출 / 분할 품질**로 분해하니, 높은 mean Dice 뒤에 **희귀 클래스 붕괴**가 숨어 있었다:

| 클래스 | M1 mean Dice | **M1 실제 검출률** |
|---|---|---|
| C1 | 0.928 | **0.0% (181개 전부 놓침)** |
| C2 | 0.981 | **0.0% (49개 전부 놓침)** |
| C3 | 0.846 | 93.4% |
| C4 | 0.983 | 96.2% |

- C1/C2의 높은 Dice는 **전부 "빈마스크 비우기"**(빈마스크 86%)에서 나온 착시 — 실제 결함은 0% 검출. min_size=0에서도 0% → **마스크 정제가 아니라 학습 붕괴**(불균형 BCE+Dice가 희귀 클래스를 빈칸 예측으로 포기).
- 교훈: **mean Dice 단독 금지, per-class 검출률 필수.**

## Stage 2 — M2: 클래스 균형 재학습 (C1/C2 살리기) ★
- 처방: **FocalTversky 손실**(FN 더 처벌) + **희귀클래스 오버샘플**(최대 21×) + **pos_weight**(C2 8×/C1 4×).

| 클래스 | M1 검출률 | **M2 검출률** | 분할 품질 |
|---|---|---|---|
| C1 | 0.0% | **83.4%** | 0.698 |
| C2 | 0.0% | **93.9%** | 0.761 |
| C3 | 93.4% | 92.8% | 0.726 |
| C4 | 96.2% | 98.1% | 0.774 |

- **mean Dice 0.9343 → 0.9431**(min_size=1200), **동시에 C1·C2를 0%→83%/94%로 회복**. 흔한 클래스(C3/C4)는 유지.
- empty FP 소폭 증가(검출↑의 트레이드오프, 허용 범위) — 이후 분류 게이트로 추가 억제 예정.
- 코드: `src/train_seg.py`(--loss focaltversky --oversample --posweight), 분해 `src/analyze_seg.py`.

## Stage 2 — M3: 종합 스윕 + 전처리 교차검증
- **스윕**(6아키텍처 × {se_resnext50, effnet-b3~b6, resnet, resnest, regnet, mit} + 전처리 8 + 손실 4 + HP) → 랭킹 = `best_dice_postproc + 0.5·min_recall`(빈마스크 착시 방지).
- **승자: DeepLabV3+/efficientnet-b3 (bce_lovasz, oversample, posweight 4,8,1,2) = 0.9514** (정제 후).
- **전처리 교차검증**(UNet/se 상위 5종 → 승자에 전이): gamma 0.9489 / highpass 0.9483 / clahe 0.9463 / tophat 0.9448 / bilateral_dog 0.9432 → **전부 control(none=0.9514) 미달**.
  - 결론: **지도 seg에선 전처리가 손해**(busbar 비지도와 정반대). 모델 용량이 전처리 이득을 흡수. → 최종 입력 = 원본.

## Stage 2 — M4: 앙상블 + TTA + 3-임계 마스크 정제 ★
- 멤버 4: DeepLabV3+/effb3 · FPN/se · UNet/se(m2_balanced) · UNet/se(tvb085). sigmoid 확률 평균.
- TTA: 원본/h-flip/v-flip 평균. 마스크 정제: max_prob(빈마스크 게이트)·min_prob(이진화)·min_area(작은마스크 제거) val 튜닝.

| | mean Dice |
|---|---|
| 단일 best (UNet/se) | 0.9361 |
| 앙상블(4)+TTA | 0.9502 |
| **+3-임계 마스크 정제** (min_prob=0.6,max_prob=0.7,min_area=600) | **0.9534** |

- per-class C1/C2/C3/C4 = [0.966, 0.992, 0.868, 0.988]. 코드: `src/postprocess.py`.

## Stage 2 — M5: 5-fold 견고성 + 분류 게이트 결합
**(1) 승자 5-fold**(누수 차단, bce_lovasz 16ep): fold0~4 = 0.9514 / 0.9528 / 0.9518 / 0.9513 / 0.9522 → **평균 0.9519, 편차 ±0.0006** = 과적합·누수 없는 견고한 일반화.

**(2) 분류 게이트**(멀티라벨 EfficientNet-B3, `src/train_clf.py`): **mAUROC 0.9954** (C1~C4 [0.995,0.997,0.993,0.997]). 결함 recall 95% 유지 시 정상(empty) 차단율 **C1 98.2% / C2 98.7% / C3 97.1% / C4 99.8%**.

**(3) 게이트 결합 = 최종**(`src/gated_eval.py`): 앙상블+TTA+마스크정제 예측에 분류기로 클래스별 빈마스크 차단(게이트 임계 C1~C4 = [0.3,0.02,0.5,0.0]).

| | mean Dice | C1 | C2 | C3 | C4 |
|---|---|---|---|---|---|
| ungated (M4) | 0.9534 | 0.966 | 0.992 | 0.868 | 0.988 |
| **+ 분류 게이트** | **0.9559** | 0.969 | 0.993 | 0.874 | 0.988 |

- **Δ +0.0025**. empty-FP가 점수의 급소인 대회 특성상, 게이트가 가장 큰 단일 레버임을 실증(특히 C1·C3 상승).
- 참고: Severstal 공개 리더보드 상위권 private ~0.90–0.918(별도 test). 본 수치는 train 이미지단위 held-out fold라 직접 비교는 아니나, **단일fold에서 0.9559는 매우 강함**.

## ① 5-fold OOF — 누수 없는 다fold 일반화 (게이트 결합)
fold0 단일 결과(0.9559)가 우연이 아님을 확인하기 위해 **OOF**(각 fold 승자 모델이 자기 val만 예측, fold별 분류기로 게이트, 임계는 fold0 값 고정(동일 임계 전이))로 **전체 train 12568장**을 평가:

| fold | n | ungated | gated |
|---|---|---|---|
| 0 | 2514 | 0.9532 | 0.9543 |
| 1 | 2514 | 0.9530 | 0.9539 |
| 2 | 2514 | 0.9518 | 0.9524 |
| 3 | 2513 | 0.9509 | 0.9523 |
| 4 | 2513 | 0.9532 | 0.9531 |
| **전체 OOF** | **12568** | **0.9524** | **0.9532** |

- per-class(gated) [0.969, 0.99, 0.870, 0.984]. fold 편차 0.9509~0.9532(±0.001) → **견고한 일반화**, 게이트는 5/5 fold 중 4개에서 +.
- 주의: OOF는 **단일 승자 모델**(deeplab/effb3) 기준(다른 3 아키텍처 멤버는 fold0만 존재). fold0 4모델 앙상블(0.9559)보다 낮은 건 앙상블 다양성이 빠졌기 때문이며, **누수 없는 일반화 수치**는 0.9532. 코드 `src/oof_gated_eval.py`.

## ③ 무라벨 이상탐지(ReconPatch/PatchCore-계열) — 효과 없음
정상(결함 없는) 이미지 패치로 메모리뱅크 구성 → 최근접 거리로 이상 점수(라벨 0). 이미지단위 AUROC:

| 설정 | AUROC | recall@FPR5% |
|---|---|---|
| resnet18 layer3 + max | 0.452 | 0.035 |
| resnet18 layer3 + top-k평균 | 0.423 | 0.027 |
| resnet18 layer2 + max | 0.394 | 0.026 |

- **모든 설정이 0.5 미만(반전)** = 정상 이미지가 오히려 더 "이상"으로 잡힘. 원인: Severstal 결함(스크래치·핀홀)이 미묘하고, **블랙보더·노출 등 도메인 아티팩트가 ImageNet 특징을 지배**해 결함 신호를 덮음.
- 다른 팀 Conv-AE도 0.7023에 그침 → **이 도메인은 무라벨 이상탐지가 근본적으로 약함**. 결론: **지도 픽셀 세그(본 프로젝트 0.95+)가 정답.**
- negative 결과도 그대로 기록. 더 끌어올리려면 full ReconPatch(contrastive+recon head, busbar처럼)+블랙보더 마스킹이 필요하나, 보조 트랙이라 여기서 종료. 코드 `src/recon_patch.py`.

## ② Pseudo-labeling 2라운드 — 긍정적(+0.0026)
승자(deeplab/effb3)로 **test 미라벨**에 의사라벨 → fold0 **train에만** 추가(val 2514 격리). 2라운드(R2는 R1 student warm-start):

| | 의사라벨수 | raw | +마스크정제(min800) | per-class |
|---|---|---|---|---|
| 단일 baseline | – | 0.9475 | 0.9514 | [0.884,0.898,0.951,0.969] |
| R1 student | 1359 | 0.9464 | 0.9518 | [0.961,0.987,0.855,0.983] |
| **R2 student** | 1397 | 0.9513 | **0.9540** | [0.964,0.990,**0.867**,0.984] |

- **단일 모델로 +0.0026**(0.9514→0.9540), 4-아키텍처 앙상블(0.9534)도 상회. C3(대형/스크래치) 검출이 가장 크게 개선.
- 누수 차단 검증: pseudo는 test 이미지뿐, val은 train과 완전 분리. 코드 `src/pseudo_label.py`.

## 최종 성능 요약
| 트랙 | mean Dice | 비고 |
|---|---|---|
| 무누수 OOF(단일·일반화) | 0.9532 | 누수 없는 일반화 수치(대표값) |
| fold0 단일+pseudo R2 | 0.9540 | 단일 모델 최고 |
| fold0 4모델 앙상블+TTA+마스크정제+게이트 | 0.9559 | |
| **fold0 5모델(+pseudo R2)+TTA+마스크정제+게이트** | **0.9573** | 최고치(앙상블 다양성 + 의사라벨 + 게이트) |

## 다음 (계획)
- pseudo R2 모델을 앙상블/게이트에 합산 → 추가 상향 기대.
- README 갱신, 배포(Pages/Docker).
- pseudo-label 2라운드(train fold 한정, 누수 격리).
- ReconPatch 이상탐지 보조(무라벨, 그들 AE 0.70 대비).
- UI/UX 콘솔(강판 업로드→4색 마스크+per-class Dice+비용절감 LIVE) + 배포.
